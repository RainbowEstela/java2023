package Practica2;

public class Ejercicio20 {

	public static void main(String[] args) {
		double a = 1, b = 2, d = 3;
		double res;
		
		//1)
		res = (5 * Math.pow(a, 2) * Math.pow(b, 3)) + (Math.sqrt((Math.pow(a, 2) + Math.pow(b, 2)))); 
		
		//2)
		res = (Math.sqrt((4 * a * Math.pow(b, 2)))) + Math.pow((a + b), 2);
		
		//3)
		res = ((Math.pow(a, 3) * b) / (2 * a * Math.pow(b, 2))) - Math.sqrt((12 * Math.pow(d, 4)));
		
	}

}
